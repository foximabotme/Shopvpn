# راه‌اندازی مرحله‌به‌مرحله Shopvpn روی Railway

این فورک، بات تلگرام و Mini App را داخل یک سرویس Railway اجرا می‌کند و تمام
اطلاعات ماندگار را روی یک Volume نگه می‌دارد.

> **تفاوت مهم با Faoxima:** Shopvpn از SQLite استفاده می‌کند؛ پس در Railway
> سرویس MySQL یا PostgreSQL نساز. فقط یک سرویس برنامه و یک Volume لازم است.

## قبل از شروع

این موارد را آماده کن:

1. اکانت GitHub و Railway؛ بهتر است در Railway با GitHub وارد شوی.
2. توکن بات از `@BotFather`.
3. آیدی عددی حساب تلگرامت؛ می‌توانی آن را از `@userinfobot` بگیری.
4. اگر همین بات جای دیگری روشن است، در پایان نصب باید نسخه قبلی را خاموش کنی؛
   یک توکن نمی‌تواند هم‌زمان در دو سرویس Polling فعال باشد.

## مرحله ۱: فورک‌کردن مخزن

1. صفحه مخزن Railway-ready را در GitHub باز کن.
2. بالا سمت راست روی **Fork** بزن.
3. **Owner** را حساب خودت و نام مخزن را مثلاً `Shopvpn-Railway` بگذار.
4. روی **Create fork** بزن و صبر کن تا مخزن زیر حساب خودت ساخته شود.

توکن، فایل `.env` و دیتابیس را هیچ‌وقت داخل GitHub آپلود نکن. این فورک آن‌ها
را در `.gitignore` قرار داده است.

## مرحله ۲: ساخت پروژه Railway

1. وارد [Railway](https://railway.com/) شو.
2. روی **New Project** و سپس **Deploy from GitHub repo** بزن.
3. فورک خودت، یعنی `USERNAME/Shopvpn-Railway`، را انتخاب کن.
4. اگر مخزن را نمی‌بینی، در اتصال GitHub به Railway دسترسی همان مخزن را فعال
   کن و صفحه را دوباره باز کن.

Deploy اول ممکن است به‌خاطر نبودن `BOT_TOKEN` و `OWNER_ID` خطا بخورد؛ در این
مرحله طبیعی است.

## مرحله ۳: اتصال Volume؛ قبل از استفاده واقعی

1. در صفحه پروژه، روی سرویس Shopvpn راست‌کلیک کن یا منوی سه‌نقطه را باز کن.
2. **Attach Volume** را انتخاب کن.
3. Mount Path را دقیقاً روی `/data` بگذار.
4. Volume را به همان سرویس Shopvpn متصل کن.

این Volume دیتابیس اصلی، دیتابیس نماینده‌ها، وضعیت گفتگوها، بکاپ‌ها، لاگ‌ها و
فایل‌های آپلودی پنل را نگه می‌دارد. بدون آن، اطلاعات با Deploy بعدی پاک می‌شود.

به‌علت SQLite و Polling:

- تعداد Replica باید دقیقاً **۱** باشد.
- **Serverless/App Sleeping** را فعال نکن.
- Deployment Overlap باید **۰** باشد؛ فایل `railway.json` این مقدار را تنظیم
  کرده است.

## مرحله ۴: ثبت متغیرهای اصلی

داخل سرویس Shopvpn به تب **Variables** برو، **Raw Editor** را باز کن و متن زیر
را با مقادیر واقعی خودت وارد کن:

```env
BOT_TOKEN=توکن_واقعی_از_BotFather
OWNER_ID=آیدی_عددی_تلگرام
BOT_MODE=polling
DATA_DIR=/data
TZ=Asia/Tehran
PYTHONUNBUFFERED=1
ADMIN_PANEL_SECRET=یک_رشته_تصادفی_طولانی_حداقل_۳۲_کاراکتر
```

برای `ADMIN_PANEL_SECRET` از Password Generator یک مدیر رمز عبور استفاده کن.
این مقدار را بعداً عوض نکن؛ تغییر آن نشست‌های پنل وب را باطل می‌کند. پس از ثبت،
از منوی سه‌نقطه همان Variable گزینه **Seal** را بزن تا مقدارش دیگر قابل مشاهده
نباشد. برای `BOT_TOKEN` هم می‌توانی همین کار را انجام دهی.

Railway بعد از ذخیره Variables یک Deploy جدید شروع می‌کند. در لاگ شروع باید
این دو خط را ببینی:

```text
[railway] starting bot
[railway] starting miniapp
```

## مرحله ۵: ساخت دامنه Mini App

1. سرویس Shopvpn را باز کن و به **Settings > Networking > Public Networking**
   برو.
2. روی **Generate Domain** بزن.
3. اگر Railway از تو Target Port خواست، پورتی را انتخاب کن که برنامه تشخیص
   داده است. برنامه روی متغیر خودکار `PORT` و آدرس `0.0.0.0` گوش می‌دهد؛ لازم
   نیست `PORT` را خودت بسازی.
4. یک آدرس شبیه `shopvpn-production-xxxx.up.railway.app` می‌گیری.

در همان Settings، بخش Deploy/Healthcheck را هم بررسی کن. مسیر Healthcheck باید
`/health` و Timeout باید `300` ثانیه باشد. `railway.json` این موارد را تنظیم
می‌کند؛ اگر در رابط Railway نمایش داده نشد، آن‌ها را دستی وارد کن.

حالا دوباره به **Variables** برو و این دو مورد را اضافه کن:

```env
MINIAPP_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}
API_BASE_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}
```

مرجع بالا باعث می‌شود دامنه Railway به‌صورت خودکار جای‌گذاری شود. اگر دامنه
سفارشی استفاده می‌کنی، به‌جای آن آدرس کامل HTTPS خودت را در هر دو مقدار بنویس.
بعد از ذخیره، Deploy جدید را تا وضعیت **Success/Active** دنبال کن.

## مرحله ۶: تست سلامت

در مرورگر این آدرس را باز کن:

```text
https://دامنه-تو/health
```

پاسخ درست این است:

```json
{"status":"ok"}
```

بعد در تلگرام وارد بات شو و `/start` بفرست. اگر بات جواب نداد، در Railway به
**Deployments > View Logs** برو و بخش عیب‌یابی پایین همین فایل را ببین.

## مرحله ۷: اتصال Mini App در BotFather

کد بات Menu Button را تنظیم می‌کند، اما برای فعال‌شدن Main Mini App در پروفایل
بات این مسیر را هم انجام بده:

1. `@BotFather` را باز کن و `/mybots` را بفرست.
2. بات را انتخاب کن.
3. وارد **Bot Settings > Configure Mini App > Enable Mini App** شو.
4. همان آدرس HTTPS Railway را وارد کن؛ مسیر اضافه و اسلش انتهایی لازم نیست.
5. در صورت درخواست نام کوتاه یا عنوان، یک نام مثل `shop` و عنوان `فروشگاه` ثبت
   کن.

حالا دکمه فروشگاه داخل بات و دکمه **Launch app** در پروفایل را تست کن.

## مرحله ۸: تنظیم فروشگاه

با حسابی که آیدی آن را در `OWNER_ID` گذاشتی `/start` بزن و از **پنل مدیریت**
موارد زیر را تنظیم کن:

1. اتصال پنل VPN و تست ارتباط.
2. دسته‌بندی، محصول، قیمت و موجودی یا Auto-Provision.
3. شماره کارت و روش‌های پرداخت.
4. متن خوش‌آمد، نام فروشگاه و ظاهر Mini App.
5. گرفتن یک بکاپ آزمایشی و اطمینان از دریافت آن در تلگرام.

کلیدهای اختیاری پرداخت و هوش مصنوعی را فقط در Railway Variables بگذار. فهرست
کامل نام‌ها در `.env.example` موجود است.

## پنل وب مستقل؛ اختیاری

Mini App خودش پنل مدیریت دارد و برای اکثر نصب‌ها کافی است. اگر پنل مستقل
`admin_panel` را هم می‌خواهی:

1. این Variables را اضافه کن:

```env
ENABLE_ADMIN_PANEL=1
ADMIN_PANEL_PORT=8002
ADMIN_PANEL_URL=https://admin.example.com
```

2. در **Settings > Networking** یک **Custom Domain** مثل
   `admin.example.com` اضافه و Target Port آن را `8002` انتخاب کن.
3. رکوردهای `CNAME` و `TXT` نمایش‌داده‌شده توسط Railway را دقیقاً در DNS دامنه
   بساز و منتظر علامت تأیید بمان.
4. Railway CLI را نصب و وارد حسابت شو، سپس داخل پوشه مخزن اجرا کن:

```bash
railway login
railway link
railway ssh -- python -m admin_panel.create_admin your_username 'a-strong-password'
```

پسورد حداقل باید ۸ کاراکتر باشد. تا قبل از ساخت حساب مالک، دامنه پنل را در
اختیار شخص دیگری قرار نده.

## انتقال اطلاعات از VPS یا نصب قبلی

روش امن برای انتقال همه‌چیز، بکاپ کامل داخلی خود بات است:

1. در بات قدیمی به **پنل مدیریت > بکاپ و بازیابی** برو.
2. **دریافت بکاپ کامل (بات اصلی + همه نماینده‌ها)** را بزن و فایل ZIP را نگه
   دار.
3. سرویس قدیمی را خاموش کن تا توکن هم‌زمان در دو جا Polling نکند.
4. نسخه Railway را با همان `BOT_TOKEN` و `OWNER_ID` بالا بیاور.
5. در بات جدید به همان بخش برو، **بازیابی کامل** را بزن و فایل ZIP را ارسال
   کن.
6. پس از پیام موفقیت، در Railway یک‌بار سرویس را **Restart** کن تا بات، Mini
   App و پنل اختیاری همگی دیتابیس بازیابی‌شده را دوباره باز کنند.
7. کاربران، سفارش‌ها، محصولات، نماینده‌ها و تنظیمات را بررسی کن و یک بکاپ تازه
   بگیر.

این فورک مسیرهای مطلق قدیمی مثل
`/root/v2ray_bot/reseller_dbs/name.db` را هنگام انتقال به
`/data/reseller_dbs/name.db` نگاشت می‌کند.

## عیب‌یابی سریع

### Deploy خطای BOT_TOKEN یا OWNER_ID دارد

هر دو Variable را بدون کوتیشن و فاصله اضافی وارد کن. `OWNER_ID` باید فقط یک
عدد باشد.

### بات خطای Conflict می‌دهد

نسخه قبلی بات هنوز روشن است، Replica بیشتر از ۱ است، یا Deployها هم‌پوشانی
دارند. نسخه قبلی را خاموش، Replica را ۱ و Overlap را ۰ کن؛ سپس Restart بزن.

### دامنه 502 یا Application Failed to Respond می‌دهد

Target Port دامنه باید همان پورتی باشد که Railway برای متغیر `PORT` تعیین
کرده است. `PORT` را دستی روی 8000 قفل نکن؛ دامنه را روی پورت تشخیص‌داده‌شده
برنامه قرار بده.

### بعد از Deploy اطلاعات پاک شد

Volume به سرویس وصل نیست یا Mount Path دقیقاً `/data` نیست. ابتدا Volume را
اصلاح کن و سپس آخرین بکاپ کامل را بازیابی کن.

### Mini App خطای initData نامعتبر می‌دهد

مطمئن شو `MINIAPP_URL` متعلق به همین سرویس است، Mini App را از داخل همان بات
باز کرده‌ای و توکن بات نماینده/اصلی با دیتابیس مربوط به خودش هماهنگ است.

### Build پایتون خطا می‌دهد

در لاگ Build بررسی کن که Railpack فایل‌های `requirements.txt`، `.python-version`
و `railpack.json` را دیده و Python 3.12 را انتخاب کرده باشد. Start Command باید
`python railway_runner.py` باشد.

## چک‌لیست نهایی

- [ ] یک سرویس Shopvpn و یک Volume با Mount Path برابر `/data`
- [ ] Replica برابر ۱ و Serverless خاموش
- [ ] `BOT_TOKEN` و `OWNER_ID` صحیح و ترجیحاً Sealed
- [ ] `BOT_MODE=polling`
- [ ] `MINIAPP_URL` و `API_BASE_URL` با HTTPS
- [ ] پاسخ موفق `/health`
- [ ] پاسخ بات به `/start`
- [ ] بازشدن Mini App از داخل تلگرام
- [ ] بکاپ آزمایشی موفق
